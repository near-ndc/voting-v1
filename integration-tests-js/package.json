{
  "name": "voting-integration-tests",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd ..; make build",
    "rebuild": "rm -rf node_modules && yarn install && yarn run build",
    "test": "ava --verbose --timeout=10m"
  },
  "devDependencies": {
    "ava": "^5.2.0",
    "near-workspaces": "^3.3.0"
  },
  "packageManager": "yarn@3.5.1"
}
